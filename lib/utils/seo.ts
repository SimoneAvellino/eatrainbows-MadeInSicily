export function title(page?: string) {
  const base = "Taormina – Lava Stone & Ceramics";
  return page ? `${page} • ${base}` : base;
}

export function description() {
  return "Artisan lava-stone tables and hand-painted ceramics from Taormina, Sicily. Maiolica-inspired, colorful, and timeless.";
}
